trigger:
  branches: { include: [ main, develop ] }

variables:
  imageName:  azdevops-cicd-sample
  imageTag:   $(Build.BuildId)

  azureSubscription: AzureSubscriptionConnection

  rgName:           ci-cd-rg
  acrName:          nithinkatruacr
  appServicePrefix: cicd-webapp
  servicePlanName:  cicd-asp
  location:         eastus                # used by az cli create

  tfStateStorageAccount: tfstateYourSuffix
  tfStateContainer:      tfstate

  sonarOrg: akhilmano1

stages:
# ─────────────────────────── Build ────────────────────────────
- stage: Build
  jobs:
  - job: Build
    pool: { name: LocalSelfHostedPool1, demands: [ "agent.os -equals Windows_NT" ] }

    steps:
    - checkout: self

    - task: AzureCLI@2
      displayName: "Login to ACR"
      inputs:
        azureSubscription: $(azureSubscription)
        scriptType: ps
        scriptLocation: inlineScript
        inlineScript: az acr login --name $(acrName)

    - task: NodeTool@0
      inputs: { versionSpec: "20.x" }

    - script: npm ci --loglevel error
      displayName: "npm ci"
      workingDirectory: $(Build.SourcesDirectory)

    - script: npm run lint
      displayName: "lint"
      workingDirectory: $(Build.SourcesDirectory)

    - script: npm test
      displayName: "unit tests"
      workingDirectory: $(Build.SourcesDirectory)

    - task: PowerShell@2
      displayName: "Build & push Docker image"
      inputs:
        targetType: inline
        workingDirectory: $(Build.SourcesDirectory)
        script: |
          docker build -t $(acrName).azurecr.io/$(imageName):$(imageTag) .
          docker push  $(acrName).azurecr.io/$(imageName):$(imageTag)

    # ----- Sonar (optional) -----
    - task: SonarCloudPrepare@3
      inputs:
        SonarCloud: SonarCloud
        organization: $(sonarOrg)
        scannerMode: CLI
        configMode: manual
        cliProjectKey: akhilmano1_azure-devops-cicd-sample-Public
        cliProjectName: CI-CD Sample
    - task: SonarCloudAnalyze@3
    - task: SonarCloudPublish@3

# ─────────────────────────── Deploy ───────────────────────────
- stage: Deploy
  dependsOn: Build
  displayName: "CD: Push container to existing App Service"

  jobs:
  - deployment: Deploy
    environment: $(Build.SourceBranchName)
    pool: { name: LocalSelfHostedPool1, demands: [ "agent.os -equals Windows_NT" ] }

    strategy:
      runOnce:
        deploy:
          steps:
          - checkout: self

                          # 1️⃣ Ensure App Service Plan + Web App exist
         - task: AzureCLI@2
  displayName: "Create plan & webapp if absent"
  inputs:
    azureSubscription: $(azureSubscription)
    scriptType: ps
    scriptLocation: inlineScript
    inlineScript: |
      $ErrorActionPreference = 'Continue'
      $rg        = "$(rgName)"
      $planName  = "$(servicePlanName)"
      $appName   = "$(appServicePrefix)-$(Build.SourceBranchName)"
      $location  = "$(location)"

      Write-Host "🔎 Checking App Service Plan '$planName'..."
      az appservice plan show -g $rg -n $planName -o none 2>$null
      if ($LASTEXITCODE -ne 0) {
        Write-Host "➕ Creating App Service Plan '$planName'"
        az appservice plan create `
          --name $planName `
          --resource-group $rg `
          --sku B1 `
          --is-linux `
          --location $location

        Write-Host "⏳ Waiting for App Service Plan '$planName' to be provisioned..."
        do {
          Start-Sleep -Seconds 5
          az appservice plan show -g $rg -n $planName -o none 2>$null
        } while ($LASTEXITCODE -ne 0)
        Write-Host "✅ Plan provisioned successfully."
      }
      else { Write-Host "✅ Plan already exists." }

      Write-Host "🔎 Checking Web App '$appName'..."
      az webapp show -g $rg -n $appName -o none 2>$null
      if ($LASTEXITCODE -ne 0) {
        Write-Host "➕ Creating Web App '$appName'"
        az webapp create `
          --resource-group $rg `
          --plan $planName `
          --name $appName `
          --deployment-container-image-name "mcr.microsoft.com/oss/nginx/nginx:latest"
        Write-Host "✅ Web App created."
      }
      else { Write-Host "✅ Web App already exists." }

          # 2️⃣ Deploy the container image
          - task: AzureWebAppContainer@1
            displayName: "Deploy container to App Service"
            inputs:
              azureSubscription: $(azureSubscription)
              resourceGroupName: $(rgName)
              appName: $(appServicePrefix)-$(Build.SourceBranchName)
              imageName: $(acrName).azurecr.io/$(imageName):$(imageTag)
